const mongoose = require('mongoose'); const Manager = require('./src/modules/manager/manager.model').default; const env = require('./src/config/env').env; mongoose.connect(env.MONGODB_URI).then(async () => { const result = await Manager.updateOne({ email: '2302030430031@silveroakuni.ac.in' }, { \: { isDeleted: false, deletedAt: null } }); console.log('Update result:', result); mongoose.disconnect(); }).catch(console.error);
